[pytest]
markers =
    powspec: Power spectrum tests
    xcor: Cross-correlation tests
    slow: Slow-running tests
    mpi: MPI-parallel tests
    ccl: Tests requiring pyccl
    app: Application tests requiring CLI dependencies
